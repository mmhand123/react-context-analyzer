[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
context-analyzer-core = { path = "../core" }
context-analyzer-engine = { path = "../engine" }
context-analyzer-frontend = { path = "../frontend" }
context-analyzer-report = { path = "../report" }
