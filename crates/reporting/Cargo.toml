[package]
name = "context-analyzer-reporting"
version = "0.1.0"
edition = "2024"

[lib]
name = "context_analyzer_reporting"
path = "src/lib.rs"

[dependencies]
context-analyzer-core = { path = "../core" }
serde_json = "1.0"
